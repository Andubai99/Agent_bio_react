# Memory Placeholder

The previous SQLite memory mechanism has been removed for the OmniParser-based
loop refactor.

This directory is intentionally documentation-only for now. Future memory work
should be designed after the new screenshot -> UI parse -> reason -> execute ->
verify loop is stable.
