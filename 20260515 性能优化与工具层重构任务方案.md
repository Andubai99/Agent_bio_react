# 2026-05-15 性能优化与工具层重构任务方案

## 0. 逐项任务列表

### A. 基线和保护

- [x] A1. 记录当前基线：选定 `logs/20260514_170423.md` 作为性能对照样本。
- [x] A2. 新增一份性能统计脚本或手工统计表，记录每轮运行的总耗时、OmniParser 启动耗时、OmniParser 解析耗时、DeepSeek 耗时、Qwen 耗时。
- [x] A3. 确认当前 `main` 分支能正常启动后端和前端。
- [x] A4. 确认 `OmniParser/.venv/`、`OmniParser/weights/`、`OmniParser/results/`、`OmniParser/imgs/` 不会进入 git。

### B. 前端控制 OmniParser

- [x] B1. 新增 `agent_server/omniparser_manager.py`，封装 OmniParser 探活、启动、停止、状态查询。
- [x] B2. 新增后端接口 `GET /omniparser/status`。
- [x] B3. 新增后端接口 `POST /omniparser/start`。
- [x] B4. 新增后端接口 `POST /omniparser/stop`。
- [x] B5. Agent 运行中禁止停止 OmniParser，返回明确错误信息。
- [x] B6. 前端 API 客户端增加 OmniParser 状态、启动、停止方法。
- [x] B7. 前端设置页增加 OmniParser 控制区，显示状态、PID、服务地址、根目录、最近探活时间。
- [x] B8. 验证手动启动 OmniParser 后再跑任务，日志出现 `OmniParser service already running`。
- [x] B9. 验证手动停止 OmniParser 后端口 `8001` 不再可用。

### C. 任务目录结构

- [ ] C1. 修改 `agent/task_loader.py`，同时支持 `tasks/*.md` 和 `tasks/<task_id>/task.md`。
- [ ] C2. 修改 `agent_server/run_manager.py` 的任务列表扫描逻辑，兼容新旧任务结构。
- [ ] C3. 将 `tasks/biopharma_protein.md` 迁移到 `tasks/biopharma_protein/task.md`。
- [ ] C4. 新建 `tasks/biopharma_protein/omniparser_cache/`。
- [ ] C5. 新建 `tasks/biopharma_protein/memory/`。
- [ ] C6. 验证前端任务列表仍能显示 `biopharma_protein`。
- [ ] C7. 验证 `run_agent.py run --task biopharma_protein --reasoner deepseek` 能加载迁移后的任务。

### D. OmniParser 解析缓存

- [ ] D1. 新增 `tools/observation_cache.py`，定义缓存路径、manifest 读写、元素 JSON 读写。
- [ ] D2. 定义缓存 key：任务 ID、步骤号、窗口标题、任务指令摘要、分辨率、OmniParser 参数。
- [ ] D3. 第一次调用 OmniParser 成功后，将截图复制到任务缓存目录。
- [ ] D4. 第一次调用 OmniParser 成功后，将项目内 `UIElement` 列表保存为元素 JSON。
- [ ] D5. 保存或更新 `manifest.json`。
- [ ] D6. `_observe()` 中先截图，再查找可用缓存。
- [ ] D7. 复用缓存前，用当前截图和缓存截图做本地差异校验。
- [ ] D8. 分辨率一致时直接复用 `pixel_bbox` 和 `center`。
- [ ] D9. 分辨率变化但比例可接受时，用 normalized bbox 重算坐标。
- [ ] D10. 缓存复用后仍执行 synthetic target 后处理。
- [ ] D11. 缓存缺失、缓存失效、缓存元素为空时自动回退 OmniParser。
- [ ] D12. 日志输出缓存命中、缓存失效原因、缓存写入路径。
- [ ] D13. 第二次运行同一任务时验证 OmniParser 调用次数减少。

### E. 元素候选筛选工具

- [ ] E1. 新增 `tools/element_candidate_filter.py`。
- [ ] E2. 实现任务文本关键词提取：引号内容、英文词组、文件名、按钮名。
- [ ] E3. 实现动作类型推断：click、double_click、input_text、checkbox、file_item、button。
- [ ] E4. 实现召回：文本命中、类型匹配、synthetic 元素、空间关系、历史成功元素。
- [ ] E5. 实现粗排：文本相似度、控件类型、空间关系、synthetic 加权、成功/失败记忆加权。
- [ ] E6. 实现精排：输入框选 label 右侧，checkbox 选 label 左侧，文件窗口选文件名。
- [ ] E7. 输出 top 30 候选，保留原始 OmniParser `idx`。
- [ ] E8. 候选置信度低时标记 `fallback_to_full_list=true`。
- [ ] E9. 第一版只写日志，不接入 DeepSeek。
- [ ] E10. 人工检查候选质量后，将 DeepSeek 输入改为候选列表。
- [ ] E11. 验证 DeepSeek 输入元素数量明显减少。
- [ ] E12. 验证候选筛选不破坏已通过实机路径。

### F. 运行记忆

- [ ] F1. 在任务目录下写入 `memory/successful_elements.jsonl`。
- [ ] F2. 在任务目录下写入 `memory/failed_candidates.jsonl`。
- [ ] F3. 动作成功后记录 step、action、target_hint、element_content、element_source。
- [ ] F4. 动作失败或验证失败后记录失败候选和失败原因。
- [ ] F5. 候选筛选工具读取成功记忆加权。
- [ ] F6. 候选筛选工具读取失败记忆降权。

### G. 工具层重构

- [ ] G1. 明确 Agent 层只负责循环调度、模型沟通和失败策略。
- [ ] G2. 将 OmniParser 服务生命周期封装到工具或后端 manager。
- [ ] G3. 将任务级缓存读写封装到 `tools/observation_cache.py`。
- [ ] G4. 将 synthetic target 后处理从 `modules/ui_parser/omniparser.py` 拆出或包装。
- [ ] G5. 将页面变化判断封装为工具接口，Agent 不直接关心阈值细节。
- [ ] G6. 将候选筛选工具接入 Reasoner 之前。
- [ ] G7. 保持每一步重构都有兼容回退路径。

### H. 验证和验收

- [ ] H1. 运行 `python -m compileall agent agent_server modules tools`。
- [ ] H2. 运行前端构建 `npm run build`。
- [ ] H3. 启动后端 `python -m agent_server`。
- [ ] H4. 启动前端 `npm run start:desktop`。
- [ ] H5. 通过前端启动 OmniParser 并确认状态为运行中。
- [ ] H6. 通过前端跑一次实机。
- [ ] H7. 分析新日志，总结总耗时和各模块耗时。
- [ ] H8. 对比基线 `148 秒`，记录优化收益。
- [ ] H9. 更新 `docs/项目结构与能力说明.md`。
- [ ] H10. 若实机通过，将本任务文档中的已完成项打勾。

## 1. 任务目标

今天的目标不是盲目压缩代码，而是把当前实机运行中最耗时、最容易重复计算的部分拆出来，形成可复用、可观测、可逐步扩展的工具层。

核心目标：

1. 让 OmniParser 的启动和关闭由前端显式控制，并实时显示服务状态。
2. 为每个任务建立独立目录，沉淀任务文档、OmniParser 截图、元素 JSON 和后续运行记忆。
3. Agent 调用 OmniParser 前优先读取并校验缓存，只有缓存缺失或失效时才重新解析。
4. 在推理层前增加元素候选筛选工具，减少输入给 DeepSeek 的元素数量。
5. 将图像后处理、页面变化判断、候选筛选、缓存读写等能力归纳到工具层，让 Agent 层只负责循环调度和模型沟通。

## 2. 当前已验证的耗时事实

参考成功实机日志 `logs/20260514_170423.md`：

- 整轮任务耗时约 148 秒。
- DeepSeek 推理约 59 秒，7 次调用，每次约 7-12 秒。
- OmniParser 约 58 秒，其中冷启动约 17 秒，5 次 UI 解析共约 41 秒。
- Qwen 视觉验证约 22 秒，5 次调用，每次约 3-8 秒。
- Maa 执行、窗口同步、本地差异判断多数在 0-1 秒级，不是主要瓶颈。

因此优先优化顺序是：

1. OmniParser 常驻和前端显式控制。
2. OmniParser 元素解析缓存。
3. 推理前元素候选筛选。
4. 视觉验证调用减少。
5. 工具层边界整理。

## 3. 当前本地规则阈值

当前页面变化判断位于 `modules/vision/page_change_detector.py`。

现有规则：

- 缩略图尺寸：`320 x 180`
- 灰度像素差异阈值：`diff_threshold = 25`
- 主区域裁剪：
  - 顶部忽略 8%
  - 底部保留到 95%
  - 左侧忽略 12%
- 计算指标：
  - `global_changed_ratio`
  - `main_changed_ratio`
  - `largest_component_ratio`

判定为页面未切换：

```text
global_changed_ratio <= 0.015
main_changed_ratio <= 0.025
largest_component_ratio <= 0.01
confidence = 0.95
local_decisive = true
```

判定为页面已切换：

```text
main_changed_ratio >= 0.20
or largest_component_ratio >= 0.12
or global_changed_ratio >= 0.25
confidence = 0.90
local_decisive = true
```

输入文本动作的特殊规则：

```text
action_type == "input_text"
main_changed_ratio <= 0.12
largest_component_ratio <= 0.05
confidence = 0.78
local_decisive = true
page_changed = false
```

中间区间：

```text
page_changed = null
confidence = 0.45
local_decisive = false
```

中间区间会生成低清 before / after / diff 对比图，并调用视觉模型兜底。

当前 OmniParser 参数：

- `OMNIPARSER_BOX_THRESHOLD = 0.05`
- `OMNIPARSER_IOU_THRESHOLD = 0.7`
- `OMNIPARSER_USE_PADDLEOCR = true`
- `OMNIPARSER_IMGSZ = 640`

## 4. 前端控制 OmniParser 生命周期

### 4.1 目标行为

前端设置区增加 OmniParser 控制区域：

- 状态显示：
  - 未启动
  - 启动中
  - 运行中
  - 停止中
  - 启动失败
- 展示字段：
  - 服务地址：`127.0.0.1:8001`
  - 进程 PID
  - 是否由本项目启动
  - 最近一次探活时间
  - 根目录：默认 `OmniParser/`
- 操作按钮：
  - 启动 OmniParser
  - 停止 OmniParser
  - 刷新状态

如果 Agent 正在运行，停止 OmniParser 应返回阻止状态，避免中途切断解析服务。

### 4.2 后端接口

在 `agent_server` 中新增 OmniParser 服务管理接口：

```text
GET  /omniparser/status
POST /omniparser/start
POST /omniparser/stop
```

返回结构建议：

```json
{
  "state": "running",
  "probe_ok": true,
  "owned": true,
  "pid": 12345,
  "host": "127.0.0.1",
  "port": 8001,
  "root": "F:\\Agent_bio_react\\OmniParser",
  "message": "OmniParser API ready"
}
```

### 4.3 与现有 Agent 的兼容方式

当前 `run_agent.py` 内部会执行：

```python
service.ensure_running()
```

如果前端已经启动了 OmniParser，Agent 会探活到已有服务并复用，不会在结束时关闭它。

因此最小改动是：

1. `agent_server` 持有一个长生命周期的 OmniParser 服务管理器。
2. 前端按钮调用 `agent_server` 控制服务。
3. `run_agent.py` 保留兜底启动逻辑。
4. 实机运行结束后不影响前端启动的 OmniParser。

这样可以立刻省掉每轮约 16-17 秒的冷启动时间。

## 5. 任务目录结构调整

### 5.1 当前问题

当前任务文件是扁平结构：

```text
tasks/
└─ biopharma_protein.md
```

当前代码中：

- `agent/task_loader.py` 的 `load_task_by_id()` 只扫描 `tasks/*.md`
- `agent_server/run_manager.py` 的 `list_tasks()` 也只扫描 `tasks/*.md`

因此要支持任务目录，需要同时改这两个入口。

### 5.2 新目录结构

建议改为：

```text
tasks/
└─ biopharma_protein/
   ├─ task.md
   ├─ omniparser_cache/
   │  ├─ manifest.json
   │  ├─ screenshots/
   │  │  ├─ step_001_home.png
   │  │  └─ step_002_intact_mass.png
   │  └─ elements/
   │     ├─ step_001_home.json
   │     └─ step_002_intact_mass.json
   └─ memory/
      ├─ successful_elements.jsonl
      └─ failed_candidates.jsonl
```

短期兼容策略：

- 继续支持旧格式 `tasks/*.md`
- 新增支持 `tasks/<task_id>/task.md`
- 迁移时先把 `tasks/biopharma_protein.md` 移到 `tasks/biopharma_protein/task.md`
- `TaskSpec.id` 仍由 frontmatter 或文件内容决定，不能因为路径变化改变任务 ID

### 5.3 缓存文件内容

`manifest.json` 建议保存：

```json
{
  "task_id": "biopharma_protein",
  "parser": {
    "box_threshold": 0.05,
    "iou_threshold": 0.7,
    "use_paddleocr": true,
    "imgsz": 640
  },
  "entries": [
    {
      "key": "step_001_main_home_2292x1314",
      "step_number": 1,
      "window_title": "Thermo BioPharma Finder 5.1",
      "instruction": "点击 `Home`。",
      "screenshot": "omniparser_cache/screenshots/step_001_home.png",
      "elements": "omniparser_cache/elements/step_001_home.json",
      "resolution": [2292, 1314],
      "created_at": "2026-05-15T15:00:00+08:00"
    }
  ]
}
```

元素 JSON 建议保存项目内部结构，而不是 OmniParser 原始全量响应：

```json
{
  "screenshot": "omniparser_cache/screenshots/step_001_home.png",
  "window_title": "Thermo BioPharma Finder 5.1",
  "image_size": [2292, 1314],
  "elements": [
    {
      "idx": 6,
      "type": "unknown",
      "content": "Home",
      "bbox": [0.01, 0.05, 0.08, 0.08],
      "pixel_bbox": [5, 72, 477, 106],
      "center": [241, 89],
      "interactive": false,
      "source": ""
    }
  ]
}
```

保留 `bbox` 是关键，因为它可以在分辨率一致或可安全缩放时重建 `pixel_bbox` 和 `center`。

## 6. OmniParser 缓存复用逻辑

### 6.1 安全原则

不能因为有旧 JSON 就直接点击旧坐标。安全复用必须满足：

1. 当前窗口标题匹配任务阶段。
2. 当前截图与缓存截图足够相似。
3. OmniParser 参数一致。
4. 分辨率一致，或可以用 normalized bbox 安全映射。
5. 缓存元素经过当前任务指令的 synthetic 后处理。

### 6.2 推荐流程

每次 `_observe()` 时：

1. Maa 先捕获当前真实截图。
2. 根据任务 ID、步骤号、窗口标题、任务指令查找缓存候选。
3. 用当前截图和缓存截图做本地缩略图差异判断。
4. 如果差异在“页面未切换”的严格区间内，复用缓存元素 JSON。
5. 如果分辨率变化，使用 normalized bbox 重新计算 `pixel_bbox` 和 `center`。
6. 对复用元素再次运行 synthetic target 后处理。
7. 如果缓存不存在、过期或差异过大，调用 OmniParser。
8. OmniParser 成功后，把截图和元素 JSON 写入任务缓存目录。

### 6.3 缓存失效条件

以下情况必须重新调用 OmniParser：

- 当前窗口标题与缓存窗口标题不匹配。
- 当前截图与缓存截图差异达到页面切换阈值。
- OmniParser 参数变化，例如 `imgsz`、`box_threshold`、`use_paddleocr`。
- 任务指令变化，且无法确认旧元素仍适用。
- 缓存元素为空或缺少目标控件。
- 上一次使用该缓存导致动作失败。

### 6.4 长期记忆雏形

`memory/successful_elements.jsonl` 保存成功点击或输入过的元素：

```json
{"step_number": 3, "action": "input_text", "target_hint": "Experiment Name", "element_content": "synthetic input_box", "element_source": "synthetic_input_box", "success": true}
```

`memory/failed_candidates.jsonl` 保存失败候选：

```json
{"step_number": 4, "action": "click", "target_hint": "...", "element_content": "More", "reason": "wrong target"}
```

后续候选筛选时可以把成功元素加权，把失败元素降权。这就是长期记忆的最小可行形态。

## 7. 推理前元素候选筛选工具

### 7.1 当前问题

当前 DeepSeek 输入中会包含 observation 里的全部元素：

```python
"omniparser_json": [
    {
        "idx": element.idx,
        "content": element.content,
        "center": element.center,
    }
    for element in context.observation.elements
]
```

成功实机中有些页面一次解析会产生 110+ 个元素。全量输入会增加模型调用时间，也会增加模型选错元素的概率。

### 7.2 新工具职责

新增元素候选筛选工具，例如：

```text
tools/element_candidate_filter.py
```

输入：

- 当前任务步骤 action
- 当前 verification
- action_sequence_hint
- window transition
- OmniParser 元素列表
- 历史成功/失败记忆

输出：

```json
{
  "candidates": [
    {
      "idx": 114,
      "score": 0.92,
      "reason": "matches input_box near Experiment Name",
      "content": "synthetic input_box: Experiment Name",
      "center": [742, 317]
    }
  ],
  "fallback_to_full_list": false
}
```

### 7.3 RAG 化思路

这里的 RAG 不一定先上向量数据库。最小可行版本可以是本地检索 + 排序：

1. 召回：
   - 从任务文字中提取引号内容，例如 `Home`、`Experiment Name`、`Add To Queue`
   - 从 action 类型推断控件类型，例如输入框、选择框、按钮、文件项
   - 根据中文方向词召回空间关系，例如左侧、右侧、下方、附近
   - 召回 synthetic 元素
   - 召回历史成功元素

2. 粗排：
   - 文本命中得分
   - 控件类型匹配得分
   - synthetic 元素加分
   - 与标签的空间关系加分
   - 历史成功加分
   - 历史失败降分

3. 精排：
   - 对 top 30 做更细的规则排序
   - 输入框优先选择 label 右侧的 synthetic input box
   - checkbox 优先选择 label 左侧的 synthetic checkbox
   - 文件选择窗口优先匹配文件名
   - 若最高分低于阈值，则回退给全量元素，保证不破坏现有成功路径

4. 交给推理器：
   - 默认只给 DeepSeek top 20-30 个候选元素
   - 保留原始 `idx`
   - 在 prompt 中说明 `idx` 来自原始 OmniParser，不是重新编号

### 7.4 预期收益

如果每次给 DeepSeek 的元素从 100+ 降到 20-30，预计：

- 单次推理耗时降低
- 输出稳定性提高
- JSON 选择错误率降低
- 日志可读性提高

## 8. 工具层重构边界

### 8.1 当前混杂点

目前部分工具逻辑散落在 Agent 或 OmniParser client 内：

- Agent 负责循环、失败处理、窗口转场、页面变化判断、验证调度。
- `modules/ui_parser/omniparser.py` 同时负责服务配置、HTTP 调用、元素转换、synthetic 后处理。
- `modules/vision/page_change_detector.py` 是独立工具，但由 Agent 直接持有。
- Maa 执行在 `tools/maa_desktop.py`。

这能跑通，但后续会让 Agent 类越来越重。

### 8.2 目标边界

Agent 层只负责：

- 读取当前任务步骤
- 调用观察工具
- 调用候选筛选工具
- 调用推理器
- 调用执行工具
- 调用验证工具
- 根据失败策略决定重试、复用缓存或退出

工具层负责：

- OmniParser 服务生命周期
- OmniParser HTTP 解析
- 任务级截图和元素 JSON 缓存
- synthetic target 后处理
- 页面变化本地判断
- 视觉验证调用
- Maa 桌面控制
- 元素候选筛选
- 运行记忆读写

### 8.3 建议目录

短期不做大规模移动，先新增工具并保持兼容：

```text
tools/
├─ maa_desktop.py
├─ omniparser_service.py
├─ observation_cache.py
├─ element_candidate_filter.py
├─ synthetic_targets.py
└─ page_change.py
```

中期再逐步把现有逻辑迁移：

- `modules/ui_parser/omniparser.py` 保留 client 外壳，内部调用工具层。
- `modules/vision/page_change_detector.py` 可迁移或包装到 `tools/page_change.py`。
- synthetic 后处理从 OmniParser client 内拆出。
- Agent 只依赖工具接口，不关心具体实现细节。

## 9. 实施顺序

### 阶段一：OmniParser 前端控制

改动范围：

- `agent_server/app.py`
- `agent_server/schemas.py`
- 新增 `agent_server/omniparser_manager.py`
- `desktop_app/src/api/client.ts`
- `desktop_app/src/App.tsx`
- `desktop_app/src/styles.css`

验收标准：

- 前端能显示 OmniParser 当前状态。
- 点击启动后，`/probe/` 返回 ready。
- 点击停止后，服务退出。
- Agent 运行中不允许停止 OmniParser。
- 已启动 OmniParser 后再跑任务，日志中出现 `OmniParser service already running`。

### 阶段二：任务目录兼容和迁移

改动范围：

- `agent/task_loader.py`
- `agent_server/run_manager.py`
- `tasks/biopharma_protein.md` 迁移为 `tasks/biopharma_protein/task.md`

验收标准：

- 前端任务列表仍能显示 `biopharma_protein`。
- `run_agent.py run --task biopharma_protein --reasoner deepseek` 能加载新路径。
- 旧的 `tasks/*.md` 仍可被识别，避免破坏兼容。

### 阶段三：OmniParser 任务缓存

改动范围：

- 新增 `tools/observation_cache.py`
- 修改 `agent/react_agent.py` 的 `_observe()`
- 修改或包装 `modules/ui_parser/omniparser.py`

验收标准：

- 第一次运行某步骤时创建截图和元素 JSON。
- 第二次运行同一任务且页面相同，日志显示复用缓存。
- 复用缓存时不出现 `Calling OmniParser UI parser`。
- 缓存失效时自动回退 OmniParser。
- 复用缓存失败不会直接导致错误点击，而是重新解析。

### 阶段四：元素候选筛选工具

改动范围：

- 新增 `tools/element_candidate_filter.py`
- 修改 `modules/reasoner/reasoner.py`
- 必要时扩展 `ReasonerContext`

验收标准：

- 日志显示原始元素数和候选元素数。
- DeepSeek 输入元素数默认不超过 30。
- 候选置信度低时自动回退全量元素。
- 成功路径任务仍能跑通。

### 阶段五：工具层整理

改动范围：

- 拆分 synthetic target 后处理
- 包装 page change detector
- 整理 Agent 依赖边界

验收标准：

- Agent 类中不再直接包含复杂图像后处理细节。
- Agent 只调工具接口和模型接口。
- 单元测试覆盖候选筛选、缓存命中、缓存失效、本地页面变化阈值。

## 10. 风险与防护

风险一：缓存过期导致错点。

防护：

- 每次复用前必须捕获当前截图并做本地差异校验。
- 缓存失败时重新 OmniParser。
- 失败候选写入 `memory/failed_candidates.jsonl` 降权。

风险二：分辨率或窗口大小变化导致坐标失效。

防护：

- 缓存保存 normalized bbox。
- 分辨率变化时重算 pixel bbox 和 center。
- 如果窗口比例变化过大，直接失效缓存。

风险三：候选筛选漏掉正确元素。

防护：

- top K 初期设为 30，不要太小。
- 低置信度回退全量元素。
- 日志保存候选筛选原因，便于人工复盘。

风险四：工具层重构影响已通过实机路径。

防护：

- 分阶段实施。
- 每阶段保留回退路径。
- 每阶段跑一次前端实机或至少脚本验证。

## 11. 今日可交付的最小可行版本

如果今天只做最小闭环，建议完成前三项：

1. 前端 OmniParser 启动、停止、状态显示。
2. 任务目录兼容和 `biopharma_protein` 迁移。
3. OmniParser 解析结果写入任务目录缓存。

候选筛选工具可以先做只读版本：

- 输入任务步骤和元素 JSON。
- 输出 top 30 候选。
- 先只写日志，不接入 DeepSeek。

确认候选质量后，再让 DeepSeek 使用筛选后的元素列表。

## 12. 成功标准

完成后，一轮任务应该能看到这些变化：

- OmniParser 不再每次冷启动。
- 重复运行同一任务时，OmniParser 调用次数明显减少。
- 日志中可以看到缓存命中或缓存失效原因。
- DeepSeek 输入元素数量明显减少。
- Agent 层代码更像调度器，工具细节逐步从 Agent 中移出。

预期性能目标：

- 短期：148 秒降低到 100-120 秒。
- 中期：通过缓存和候选筛选降低到 80-100 秒。
- 长期：结合更稳定的任务记忆和更少视觉兜底，进入 60-80 秒区间。







> AI 忽视这部分，仅作用户记录提示词用：
> 很好，你现在给我把这几个解决方案写到一个md文档中，放在项目根目录下，这个任务就是我们今天5.15的任务。
>
>  我还有一些要求：OmniParser的启动和关闭应该在前端有一个按钮负责，并且实时显示它的开启状态；应该根据任务，创建一个专门放置OmniParser解析元素json的地方，我的想法是把tasks/里的任务再放入更深的文件夹中，以任务名称命名，在这个文件夹中保存的有任务md文档、OmniParser解析元素的截图和对应的元素json文件，之后调用OmniParser的时候，agent可以根据任务来自主地读取并复用已有的截图或json，如果没有这样的缓存的话agent就会创建并完善这些缓存，这一块的逻辑具体你可以帮我想，这样就可以有效减少OmniParser的重复调用，同时也可以作为后续agent长期记忆的一部分的雏形；对于推理层，需要做一个工具，这个工具将在每次获取json文件后由Agent自动地调用，就用来做元素候选筛选，可以考虑用RAG的思想，做粗排、精排和召回；本地规则方面，你需要告诉我现在我们的阈值是多少？ 
>
> 而且我觉得我们之前做图像后处理之类的Agent附加内容也应该都统一归纳到工具层，让Agent层只作为一个负责与大模型沟通或根据内在逻辑指示调用工具完成任务的循环，这样的架构比较清晰。 请你认真思考上面的要求，给我写一份经过验证地具有可行性的任务方案。
