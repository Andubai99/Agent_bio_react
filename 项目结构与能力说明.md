# 项目结构与能力说明

本文档梳理当前 `Agent_bio_react` 项目的目录结构、运行链路、模块职责、已实现能力和当前边界。内容按当前代码状态整理。

## 一、项目定位

本项目是面向 Thermo BioPharma Finder 5.1 的桌面智能体。它按照任务文件中的步骤循环运行，每轮围绕“截图 -> UI 解析 -> 推理 -> 执行 -> 验证”推进。

当前主循环为：

1. 根据任务中的 `窗口：` 字段确认当前应该聚焦的窗口。
2. 连接或恢复目标窗口，并置前。
3. 截图。
4. 调用 OmniParser 解析截图，得到 UI 元素列表。
5. UI 后处理层根据当前任务语义补充 synthetic 控件，例如输入框、选择框。
6. 推理层接收当前任务、动作序列提示和 UI 元素列表，返回 `actions` 动作数组。
7. 执行层按 `actions` 顺序依次执行点击、输入、双击等动作。
8. 如果任务要求窗口切换，动作后轮询等待目标窗口出现，并切换 Maa 控制器绑定窗口。
9. 截取动作后页面。
10. 本地页面变化检测判断页面是否整体切换。
11. 视觉模型验证当前任务指示是否完成。
12. `verified=true` 时当前任务指示序号加 1，否则当前任务最多重试一次。
13. 回到下一轮截图或复用观察结果。

如果 `page_changed=false`，下一轮会复用上一轮截图和 UI 元素列表，不重新调用 OmniParser。这样可以减少不必要的解析开销。

## 二、根目录结构

```text
Agent_bio_react/
├─ agent/                         # 智能体主循环、任务解析、任务加载、运行日志、核心数据类型
├─ modules/                       # 可复用能力模块
│  ├─ reasoner/                   # 推理层：DeepSeek / 手动 / 脚本推理器
│  ├─ ui_parser/                  # UI 解析层：OmniParser 接入和 synthetic 控件后处理
│  ├─ vision/                     # 视觉层：页面变化检测、视觉模型 provider、完成验证
│  ├─ memory/                     # memory 机制占位，不实现
│  └─ api_keys.py                 # API key 读取工具
├─ tools/                         # 桌面执行层：Maa Win32 控制器封装
├─ config/                        # 本项目自己的配置
│  └─ settings.yaml               # Win32 截图、输入、窗口同步等待配置
├─ tasks/                         # 任务文件
│  └─ biopharma_protein.md
├─ tests/                         # 调试样例和临时输出，不是完整自动化测试集
├─ docs/                          # 架构图、错误处理占位文档
├─ data/                          # 运行时截图、日志、页面变化对比图等输出
├─ run_agent.py                   # Python 启动入口
├─ run_agent.ps1                  # PowerShell 启动脚本
├─ requirements.txt               # Python 依赖
├─ 工作日志.md                    # 实机运行记录
└─ 项目结构与能力说明.md
```

## 三、任务文件格式

任务文件位于 `tasks/`，当前主任务是：

```text
tasks/biopharma_protein.md
```

当前任务格式由三类字段组成：

```text
窗口定义：
- 主窗口：Thermo BioPharma Finder 5.1
- 添加分析文件窗口：Add Analysis File(s)

任务流程：
0. 动作：点击 `Home`。
   窗口：主窗口
   验证：回到 `Home` 界面。
```

字段含义：

- `窗口定义：`：把任务中的窗口别名映射为真实窗口标题。
- `动作：`：发送给推理器，用于决定要执行的 UI 动作。
- `窗口：`：由 Agent 使用，用于判断当前应该聚焦哪个窗口，以及动作后是否需要切换窗口。
- `验证：`：发送给视觉验证层，用于判断当前任务指示是否完成。

`窗口：主窗口 -> 添加分析文件窗口` 表示动作前应该在 `Thermo BioPharma Finder 5.1` 窗口，动作后应该等待并切换到 `Add Analysis File(s)` 窗口。

## 四、agent 目录

```text
agent/
├─ react_agent.py                 # ReActAgent 主循环
├─ task_loader.py                 # Markdown 任务加载
├─ task_parser.py                 # 任务结构解析、窗口转场解析、动作序列提示解析
├─ runtime_logger.py              # 控制台/空日志器和日志摘要
├─ types.py                       # ImageInfo、UIElement、AgentAction、ReasonerDecision 等核心类型
└─ __init__.py
```

### `react_agent.py`

`ReActAgent` 是主调度器，负责：

- 连接初始目标窗口。
- 维护当前任务指示序号。
- 读取每一步的 `动作 / 窗口 / 验证`。
- 动作前检查当前窗口是否符合任务要求。
- 截图并调用 UI 解析层。
- 调用推理层。
- 将推理结果统一为 `actions` 动作序列。
- 对每个动作做 synthetic 控件重定向兜底。
- 按顺序执行动作序列。
- 动作后等待目标窗口出现。
- 执行本地页面变化检测。
- 调用视觉验证。
- 根据 `verified` 和 `page_changed` 决定推进、复用观察、重试或退出。

### `task_parser.py`

当前实现三类解析：

- `parse_task_body()`：解析 `窗口定义` 和每个编号任务步骤。
- `parse_window_transition()`：解析 `主窗口 -> 添加分析文件窗口` 这样的窗口转场。
- `parse_action_sequence()`：从自然语言动作中生成 `action_sequence_hint`。

例如：

```text
点击 `Experiment Name` 右侧输入框并输入 `BioPharma test demo1`。
```

会被解析为：

```json
[
  {
    "order": 1,
    "action": "click",
    "target_hint": "Experiment Name 右侧输入框",
    "text": null
  },
  {
    "order": 2,
    "action": "input_text",
    "target_hint": "Experiment Name 右侧输入框",
    "text": "BioPharma test demo1"
  }
]
```

这个结果不会直接执行，只作为推理器的参考提示。最终执行动作仍以推理器返回的 `actions` 为准。

## 五、推理层

```text
modules/reasoner/
├─ reasoner.py
└─ __init__.py
```

当前支持：

- `DeepSeekReasoner`
- `ManualReasoner`
- `ScriptedReasoner`
- `NullReasoner`

### 发送给 DeepSeek 的内容

推理层的 user message 当前包含：

```json
{
  "task": {
    "id": "biopharma_protein:step_3",
    "title": "任务：蛋白 (3/7)",
    "current_instruction": "点击 `Experiment Name` 右侧输入框并输入 `BioPharma test demo1`。",
    "current_instruction_index": 3,
    "current_instruction_total": 7,
    "action_sequence_hint": [
      {
        "order": 1,
        "action": "click",
        "target_hint": "Experiment Name 右侧输入框",
        "text": null
      },
      {
        "order": 2,
        "action": "input_text",
        "target_hint": "Experiment Name 右侧输入框",
        "text": "BioPharma test demo1"
      }
    ],
    "return_format": {
      "top_level_fields": ["actions"],
      "action_item_fields": ["action", "index", "coordinate", "text"]
    }
  },
  "omniparser_json": [
    {
      "idx": 110,
      "content": "Experiment Name input box",
      "center": [632, 254]
    }
  ]
}
```

只发送 UI 元素必要字段：

- `idx`
- `content`
- `center`

不发送 `bbox`、`pixel_bbox`、`type`、`source`、`interactivity` 等字段。

### DeepSeek 标准返回格式

当前标准返回格式统一为：

```json
{
  "actions": [
    {
      "action": "click",
      "index": 110,
      "coordinate": [632, 254],
      "text": null
    },
    {
      "action": "input_text",
      "index": 110,
      "coordinate": [632, 254],
      "text": "BioPharma test demo1"
    }
  ]
}
```

规则：

- 顶层只允许 `actions`。
- 单动作也必须放进 `actions` 数组。
- 多动作不限制数量，按照 `action_sequence_hint.order` 和任务原文语序排列。
- 每个 `index` 必须来自当前 `omniparser_json[].idx`。
- 每个 `coordinate` 必须逐字复制同一个元素的 `center`。
- 非 `input_text` 动作的 `text` 必须为 `null`。

代码里仍保留旧格式兼容：

```json
{"action": "click", "index": 1, "coordinate": [x, y], "text": null}
```

旧格式会被立即转换为 `actions`，但新 system prompt 已要求模型不要返回旧格式。

## 六、UI 解析层

```text
modules/ui_parser/
├─ omniparser.py
└─ __init__.py
```

职责：

- 启动或连接 OmniParser FastAPI 服务。
- 发送截图给 OmniParser。
- 将 OmniParser 返回项转换为 `UIElement`。
- 根据当前任务语义补充 synthetic 控件。
- 为推理层提供简化 UI 元素列表。

### OmniParser 服务

Agent 启动时会自动启动或连接 OmniParser 服务。用户不需要手动启动 FastAPI。

默认配置：

```text
OMNIPARSER_HOST=127.0.0.1
OMNIPARSER_PORT=8001
OMNIPARSER_ROOT=F:\OmniParser
```

### synthetic target 机制

当前输入框和选择框已经统一为 synthetic target 后处理机制。

处理流程：

1. 从当前任务动作和验证文本中判断是否需要补充 synthetic 控件。
2. 解析目标标签，例如 `Experiment Name`、`Default ReSpect`。
3. 根据 `左侧 / 右侧` 等语义确定搜索方向。
4. 在标签附近扫描真实控件。
5. 将检测到的控件追加为新的 `UIElement`。

当前支持：

- `input_box`
- `checkbox`

示例 synthetic 输入框：

```json
{
  "idx": 110,
  "content": "Experiment Name input box",
  "center": [632, 254]
}
```

示例 synthetic 选择框：

```json
{
  "idx": 116,
  "content": "Default ReSpect checkbox",
  "center": [1247, 799]
}
```

选择框检测已改为基于灰色边框连通域识别，避免把表格线或单元格边框误当成 checkbox。

### synthetic 控件重定向

如果推理器仍然选择了标签文本，例如 `Default ReSpect`，而当前任务语义要求点击它左侧选择框，Agent 会在执行前尝试重定向到对应 synthetic 控件。

这是一层兜底。理想情况下，推理器会直接选择 synthetic 元素。

## 七、桌面执行层

```text
tools/
├─ maa_desktop.py
└─ __init__.py
```

`MaaDesktop` 直接使用当前环境安装的 `maa` 包操作 Win32 窗口，不再导入或复用旧项目代码。

当前支持动作：

- `click`
- `double_click`
- `input_text`
- `drag`
- `press_key`
- `wait`
- `noop`

执行层只负责交互，不负责视觉判断。

### 坐标原则

执行层点击的是 `UIElement.center`，并通过当前截图尺寸和 Maa controller 分辨率做映射。

当前配置使用 raw size 截图：

```yaml
screenshot_use_raw_size: true
screenshot_target_long_side: 0
```

正常情况下截图像素尺寸与 controller 分辨率一致，不再复用旧项目的截图缩放配置。

### 窗口同步与弹窗等待

窗口定义来自任务文件：

```text
添加分析文件窗口：Add Analysis File(s)
```

动作后如果任务要求窗口转场，例如：

```text
窗口：主窗口 -> 添加分析文件窗口
```

Agent 会等待标题包含 `Add Analysis File(s)` 的顶层窗口出现，然后重新绑定 Maa controller。

`Add Analysis File(s)` 不是独立进程，通常是 `BioPharma.exe` 进程创建的模态/文件选择窗口，所以任务管理器里不一定能看到独立进程。

当前窗口同步已改为轮询等待，解决弹窗出现较慢的问题：

```yaml
window_sync_timeout_seconds: 8.0
window_sync_poll_interval_seconds: 0.25
```

也可以通过环境变量覆盖：

```powershell
$env:BIO_REACT_WINDOW_SYNC_TIMEOUT_SECONDS="12"
$env:BIO_REACT_WINDOW_SYNC_POLL_INTERVAL_SECONDS="0.25"
```

观察前窗口检查保持 0 秒超时，用于快速确认当前窗口上下文；动作后的窗口转场才使用较长等待。

## 八、视觉层

```text
modules/vision/
├─ providers.py
├─ schemas.py
├─ verifier.py
├─ page_change_detector.py
└─ __init__.py
```

视觉模型当前统一返回：

```json
{
  "verified": true,
  "page_changed": false
}
```

字段含义：

- `verified`：当前任务指示是否已经完成。
- `page_changed`：页面是否发生整体切换。

### 本地页面变化检测

`page_change_detector.py` 会比较动作前后截图，输出：

- `page_changed=true`
- `page_changed=false`
- `page_changed=null`

以及：

- `confidence`
- `source`
- `reason`
- `metrics`
- `local_decisive`

当 `local_decisive=true`：

- Agent 采用本地 `page_changed`。
- 视觉模型只负责判断 `verified`。

当 `local_decisive=false`：

- Agent 生成低清 `before | after | diff` 对比图。
- 调用视觉模型一次性判断 `verified` 和 `page_changed`。

## 九、配置

主配置文件：

```text
config/settings.yaml
```

当前内容：

```yaml
win32:
  screencap_method: Background
  mouse_method: Seize
  keyboard_method: Seize
  screenshot_use_raw_size: true
  screenshot_target_long_side: 0
  window_sync_timeout_seconds: 8.0
  window_sync_poll_interval_seconds: 0.25
```

这个仓库只读取自己的 `config/settings.yaml`，不再读取旧项目配置。

## 十、启动方式

推荐 PowerShell 命令：

```powershell
cd /d F:\Agent_bio_react
.\run_agent.ps1 run --task biopharma_protein --reasoner deepseek --max-steps 30 --summary-only
```

Python 入口：

```powershell
python run_agent.py run --task biopharma_protein --reasoner deepseek --max-steps 30 --summary-only
```

启动流程：

1. 加载任务文件。
2. 创建推理器。
3. 启动或连接 OmniParser 服务。
4. 创建视觉 provider。
5. 创建 `VisionVerifier`。
6. 创建 `MaaDesktop`。
7. 创建并运行 `ReActAgent`。

## 十一、运行数据

运行时输出主要在：

```text
data/
├─ screenshots/       # 每轮截图、动作后截图、完成检查截图
├─ page_change/       # 页面变化低清对比图
└─ run_logs/          # 实机运行原始日志
```

根目录：

```text
工作日志.md
```

用于记录实机运行结论、失败点和关键动作。

## 十二、当前已验证能力

当前代码已经具备：

- 自动连接 Thermo 主窗口。
- 自动启动或连接 OmniParser FastAPI 服务。
- 按任务步骤维护当前任务指示。
- 支持读取 `窗口定义` 并处理窗口别名。
- 支持 `主窗口 -> 添加分析文件窗口` 这类窗口转场。
- 动作后轮询等待慢弹出的窗口。
- 每轮截图并解析 UI 元素。
- 根据任务语义补充 synthetic 输入框和选择框。
- 推理层标准返回 `actions` 动作数组。
- 支持一个任务指示中返回多个动作。
- 支持按顺序执行动作序列。
- 支持点击输入框并输入文本这样的组合动作。
- 校验推理返回的 `index` 和 `coordinate`。
- 支持点击、双击、输入文本、拖拽、按键、等待、noop。
- 本地页面变化检测。
- 本地确定时减少视觉模型页面变化判断开销。
- 本地不确定时调用低清对比图做统一视觉验证。
- 视觉模型返回严格限定为 `{"verified": boolean, "page_changed": boolean}`。
- `verified=true` 后任务指示加 1。
- `verified=false` 时当前任务最多重试一次。
- `page_changed=false` 时复用上一轮观察结果。

最近一次实机验证中，多动作机制已成功：

```json
{
  "actions": [
    {"action": "click", "index": 110, "coordinate": [632, 254], "text": null},
    {"action": "input_text", "index": 110, "coordinate": [632, 254], "text": "BioPharma test demo1"}
  ]
}
```

Agent 按顺序执行了：

1. 点击 `Experiment Name input box`
2. 输入 `BioPharma test demo1`

并通过视觉验证。

## 十三、当前边界和待办

当前仍有这些边界：

- 错误处理层只有文档占位，还没有统一实现。
- memory 机制已删除，仅保留占位目录。
- `parse_action_sequence()` 是轻量规则解析，只提供提示，不应当被视为强语义规划器。
- 页面变化检测阈值仍是经验值，需要更多实机样本校准。
- 视觉验证依赖 Qwen 视觉模型和 API key。
- 推理层依赖 DeepSeek API key。
- 桌面执行依赖本机 Thermo BioPharma Finder 窗口、Maa Win32 支持和当前 Windows 桌面状态。
- 文件选择窗口 `Add Analysis File(s)` 是同一进程内弹窗，不是独立进程；如果系统状态阻止弹出，窗口同步仍会超时失败。

## 十四、分层总结

```text
任务层 tasks/
  ↓
任务解析 agent/task_parser.py
  ↓
主循环 agent/react_agent.py
  ↓
观察层 modules/ui_parser/omniparser.py
  ↓
推理层 modules/reasoner/
  ↓
执行层 tools/maa_desktop.py
  ↓
窗口同步 tools/maa_desktop.py
  ↓
页面变化检测 modules/vision/page_change_detector.py
  ↓
视觉完成验证 modules/vision/verifier.py
  ↓
循环控制 agent/react_agent.py
```

当前结构已经把推理层、视觉层、UI 解析层和 memory 占位统一放入 `modules/`。`agent/` 只负责任务状态、循环调度和跨模块协作。
