# 项目结构与能力说明

本文档梳理当前 `Agent_bio_react` 项目的代码目录、运行链路、模块职责和已有能力。

## 一、项目定位

本项目构建的是一个面向 Thermo BioPharma Finder 软件的桌面智能体。智能体按照任务文件中的步骤循环运行：

1. 连接目标窗口并置前。
2. 截图。
3. 调用 OmniParser 解析截图，得到 UI 元素列表。
4. 推理层根据当前任务指示和 UI 元素列表选择下一步交互元素。
5. 执行层根据元素中心坐标执行点击、输入等动作。
6. 本地页面变化检测判断页面是否整体切换。
7. 视觉完成验证层判断当前任务指示是否完成。
8. 如果完成，则任务指示序号加 1；否则当前任务指示最多重试一次。
9. 回到下一轮循环。

其中 `page_changed=false` 时，下一轮复用上一轮截图和 UI 元素列表，不重新调用 OmniParser。

## 二、根目录结构

```text
Agent_bio_react/
├─ agent/                  # 智能体主循环、任务加载、运行日志、核心数据类型
├─ modules/                # 可复用能力模块
│  ├─ reasoner/            # 推理层：DeepSeek / 手动 / 脚本推理器
│  ├─ vision/              # 视觉层：视觉模型、完成验证、页面变化检测
│  ├─ ui_parser/           # UI 解析层：OmniParser 接入和元素后处理
│  ├─ memory/              # memory 机制占位
│  └─ api_keys.py          # API key 读取工具
├─ tools/                  # 桌面执行层
├─ tasks/                  # 任务文件
├─ tests/                  # 当前保留的测试/调试输入输出样例
├─ docs/                   # 架构图和错误处理占位文档
├─ data/                   # 运行时截图、日志、页面变化对比图等输出
├─ run_agent.py            # Python 启动入口
├─ run_agent.ps1           # PowerShell 启动脚本，设置默认环境变量
├─ requirements.txt        # Python 依赖
└─ 工作日志.md             # 实机运行记录
```

## 三、核心目录说明

### `agent/`

`agent/` 保存智能体主体逻辑，不再保存推理模型代码。

```text
agent/
├─ react_agent.py          # ReActAgent 主循环
├─ task_loader.py          # Markdown 任务加载
├─ runtime_logger.py       # 控制台/空日志器和日志摘要
├─ types.py                # ImageInfo、Observation、ToolResult 等核心类型
└─ __init__.py
```

`react_agent.py` 的主要职责：

- 连接窗口。
- 管理当前任务指示序号。
- 管理观察缓存。
- 调用 OmniParser 或复用上一轮观察。
- 调用推理层选择动作。
- 调用桌面执行层执行动作。
- 调用本地页面变化检测。
- 调用视觉完成验证。
- 根据 `verified` 和 `page_changed` 决定是否进入下一步、重试或退出。

### `modules/reasoner/`

推理层已经从 `agent/reasoner.py` 拆出，放入：

```text
modules/reasoner/
├─ reasoner.py             # 推理器实现
└─ __init__.py             # 导出推理器类型
```

当前支持：

- `DeepSeekReasoner`
- `ManualReasoner`
- `ScriptedReasoner`
- `NullReasoner`

DeepSeek 推理层的输入主要是：

```json
{
  "task": "当前任务指示（x/y）：...",
  "omniparser_json": [
    {
      "idx": 1,
      "content": "...",
      "center": [x, y]
    }
  ]
}
```

DeepSeek 被要求严格返回：

```json
{
  "action": "click",
  "index": 1,
  "coordinate": [x, y],
  "text": null
}
```

执行层会校验：

- `index` 必须存在于当前 UI 元素列表。
- `coordinate` 必须与对应元素的 `center` 完全一致。
- 非 `input_text` 动作会忽略 `text` 字段。

### `modules/vision/`

视觉层已经整体移动到：

```text
modules/vision/
├─ providers.py            # Qwen/临时视觉 provider
├─ schemas.py              # 视觉验证返回格式的统一定义
├─ verifier.py             # 统一视觉验证入口
├─ page_change_detector.py # 本地页面变化检测
└─ __init__.py
```

视觉模型的返回格式只有一种，由 `modules/vision/schemas.py` 统一限定：

```json
{
  "verified": true,
  "page_changed": false
}
```

字段含义：

- `verified`：当前任务指示是否已经完成。
- `page_changed`：页面是否发生整体切换。

agent 根据本地页面变化结果选择性解析：

- 如果本地返回 `local_decisive=true`，agent 只采用模型的 `verified`，`page_changed` 使用本地结果覆盖。
- 如果本地返回 `local_decisive=false` 且 `page_changed=null`，agent 使用低清 `before | after | diff` 合成图调用视觉模型，同时采用模型返回的 `verified` 和 `page_changed`。

### `modules/vision/page_change_detector.py`

本地页面变化检测用于降低视觉 token 成本。

它会：

- 读取动作前截图和动作后截图。
- 缩放到低分辨率。
- 转灰度。
- 计算全图变化比例。
- 计算主区域变化比例。
- 计算最大变化连通块比例。
- 输出三态结果：
  - `page_changed=true`
  - `page_changed=false`
  - `page_changed=null`
- `page_changed=true/false` 时会同时输出 `local_decisive=true`，agent 把本地判断作为最终页面变化结论。

如果结果是 `null`，agent 会生成低清对比图，并调用统一视觉验证，让模型一次返回 `verified` 和 `page_changed`。

### `modules/ui_parser/`

```text
modules/ui_parser/
├─ omniparser.py
└─ __init__.py
```

职责：

- 启动或连接 OmniParser FastAPI 服务。
- 发送截图给 OmniParser。
- 把 OmniParser 返回内容转换成 `UIElement`。
- 输出给推理器的元素只保留必要字段：`idx`、`content`、`center`。
- 支持输入框后处理：
  - 当任务中出现“输入框/文本框/input box”等关键词时启用。
  - 根据任务中提到的标签，例如 `Experiment Name`，搜索其右侧白色输入框。
  - 支持检测多个输入框并加入元素列表。
  - `content` 形式为：`<label> input box` 或 `<label> input box <rank>`。

### `tools/`

```text
tools/
├─ maa_desktop.py
└─ __init__.py
```

桌面执行层只负责桌面交互，不再负责视觉验证。

当前能力：

- 连接窗口。
- 置前窗口。
- 截图。
- 点击元素。
- 双击元素。
- 输入文本。
- 拖拽元素。
- 按键。
- 等待。

`MaaDesktop` 直接使用当前环境安装的 `maa` 包操作 Win32 窗口，不再导入或读取旧工程内容。
桌面执行配置位于 `config/settings.yaml`，当前默认使用 raw size 截图，使截图像素尺寸与 Maa controller 分辨率保持一致。

### `tasks/`

```text
tasks/
└─ biopharma_protein.md
```

任务文件保留任务步骤和动作要求。agent 会把任务正文拆分成编号步骤，并维护当前任务指示。

当前任务指示只有在视觉完成验证 `verified=true` 后才会加 1。

### `tests/`

```text
tests/
└─ OmniParser/
   ├─ Home_image.png
   └─ Home_json.json
```

当前不是完整自动化测试集，而是保留 OmniParser 调试样例：

- `Home_image.png`：Thermo 软件截图样例。
- `Home_json.json`：OmniParser 元素列表样例。

### `modules/memory/`

当前 memory 机制已删除，仅保留占位目录和说明文件。

### `docs/`

```text
docs/
├─ 架构.png
└─ ERROR_HANDLING_TODO.md
```

错误处理层目前是占位，不实现具体代码。后续应把窗口连接、截图、OmniParser、推理、执行、视觉验证等步骤统一接入错误处理层。

## 四、启动入口

### PowerShell 启动

```powershell
.\run_agent.ps1 run --task biopharma_protein --reasoner deepseek --max-steps 30
```

`run_agent.ps1` 会设置默认环境变量，例如：

- `DEEPSEEK_REASONER_THINKING=disabled`
- `QWEN_VISION_ENABLE_THINKING=false`
- `OMNIPARSER_HOST=127.0.0.1`
- `OMNIPARSER_PORT=8001`
- `OMNIPARSER_TIMEOUT_SECONDS=300`

### Python 启动

```powershell
python run_agent.py run --task biopharma_protein --reasoner deepseek
```

启动时会：

1. 加载任务。
2. 创建推理器。
3. 启动或连接 OmniParser 服务。
4. 创建视觉 provider。
5. 创建 `VisionVerifier`。
6. 创建 `MaaDesktop`。
7. 创建 `ReActAgent`。
8. 执行智能体主循环。

## 五、当前智能体主循环能力

当前主循环已经具备：

- 自动连接目标 Thermo 窗口。
- 自动启动 OmniParser FastAPI 服务。
- 每轮截图并解析 UI 元素。
- 按当前任务指示调用推理器。
- 只向推理器发送必要 UI 信息：`idx`、`content`、`center`。
- 校验推理器返回的元素序号和坐标。
- 支持点击、双击、输入文本、拖拽、按键、等待。
- 动作后截图。
- 本地判断页面是否切换。
- 本地确定时只用视觉模型判断当前任务指示是否完成，页面变化以本地结果为准。
- 本地不确定时用低清对比图调用统一视觉验证，一次返回 `verified` 和 `page_changed`。
- `verified=true` 时任务指示加 1。
- `verified=false` 时当前任务指示重试一次，再失败则退出。
- `page_changed=false` 时下一轮复用上一轮截图和 UI 元素列表，不调用 OmniParser。

## 六、当前能力边界

当前仍有这些边界或占位：

- 错误处理层只有文档占位，还未统一实现。
- memory 机制已删除，仅保留占位。
- 页面变化本地检测阈值是工程经验值，后续需要更多实机样本校准。
- 统一视觉验证依赖 Qwen 视觉模型和 API key。
- DeepSeek 推理层依赖 DeepSeek API key。
- 桌面执行依赖当前环境安装的 `maa` 包和 `config/settings.yaml`。
- 实机任务依赖 Thermo BioPharma Finder 软件窗口存在且可交互。

## 七、当前代码分层总结

```text
任务层 tasks/
  ↓
agent/react_agent.py
  ↓
观察层 modules/ui_parser/omniparser.py
  ↓
推理层 modules/reasoner/
  ↓
执行层 tools/maa_desktop.py
  ↓
页面变化检测 modules/vision/page_change_detector.py
  ↓
视觉完成验证 modules/vision/verifier.py
  ↓
循环控制 agent/react_agent.py
```

当前结构已经把推理层、视觉层、UI 解析层和 memory 占位从根目录/agent 主体中拆出，统一放入根目录 `modules/` 下，agent 主体只负责调度、状态推进和循环控制。
